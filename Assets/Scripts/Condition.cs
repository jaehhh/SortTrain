public enum Hat { none, have, hatCount}
public enum Top { red, green, blue, purple, brown, topCount}
public enum Pants {red, green, blue, purple, brown, pantsCount}

public enum ToKill { kill, nokill, toKillCount}

public enum Condition { hat, top, pants, conditionCount}